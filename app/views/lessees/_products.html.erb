<ul>
  <% products.each do |product| %>
    <li><%= product.identifier %> <%= link_to(lessee_product_path(lessee, product), method: :delete, class: "btn btn-default") do %>
        <span class="glyphicon glyphicon-minus" aria-hidden="true"></span> Remove
      <% end %>
    </li>
  <% end %>
  <% if lessee.not_products.present? %>
    <li>
      <%= form_tag(lessee_products_path(lessee), method: :post) do %>
        <%= select_tag "id", options_from_collection_for_select(lessee.not_products, "id", "identifier") %>
        <%= button_tag(type: 'submit',  class: 'btn btn-default') do %>
          <span class="glyphicon glyphicon-plus" aria-hidden="true"></span> Add
        <% end %>
      <% end %>
    </li>
  <% end %>
</ul>
