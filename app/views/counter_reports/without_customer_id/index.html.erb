<% content_for :head do %>
  <meta name="turbolinks-cache-control" content="no-cache">
<% end %>

<h1>COUNTER 5 Reports</h1>

<h2>Platform Usage (PR_P1)</h2>
<%= render 'counter_reports/without_customer_id/pr_p1/form' %>
<h2>Title Master Report (TR)</h2>
<%= render 'counter_reports/without_customer_id/tr/form' %>
<h2>Book Requests (Excluding OA_Gold) (TR_B1)</h2>
<%= render 'counter_reports/without_customer_id/tr_b1/form' %>
