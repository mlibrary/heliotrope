<%
  products = Product.filter(identifier_like: params[:identifier_like], name_like: params[:name_like]).order(identifier: :asc).page(params[:page])
  size = 12
%>
<h1>Products</h1>
<form name="filter" method="get" action="<%= fulcrum_partials_path(:products) %>">
  <table class="table table-striped" summary="Product List">
    <tr>
      <th>
        <%= link_to(new_greensub_product_path) do %>
          <span class="glyphicon glyphicon-plus" aria-hidden="true"></span> New
        <% end %>
      </th>
      <th><label for="identifier_like">Identifier</label></th>
      <th><label for="name_like">Name</label></th>
      <th>Components</th>
      <th>Subscribers</th>
    </tr>
    <tr>
      <td><button name="submit" type="submit" value="filter">Filter</button></td>
      <td><input type="text" name="identifier_like" value="<%= params[:identifier_like] %>" size="<%= size %>" aria-label="identifier_like"></td>
      <td><input type="text" name="name_like" value="<%= params[:name_like] %>" size="<%= size %>" aria-label="name_like"></td>
      <td></td>
      <td></td>
    </tr>
    <% products.each do |product| %>
      <tr>
        <td><%= link_to(greensub_product_path(product)) do %>
            <span class="glyphicon glyphicon-sunglasses" aria-hidden="true"></span> Show
          <% end %>
        </td>
        <td><%= product.identifier %></td>
        <td><%= product.name %></td>
        <td><%= product.components.count %></td>
        <td><%= Greensub.product_subscribers(product).count %></td>
      </tr>
    <% end %>
  </table>
</form>
<%= paginate products %>
