<% if (Settings.host == "www.fulcrum.org") && (controller.current_institutions.map(&:identifier) & ["490", "2405", "2334", "2402"]).empty? %>
<!-- Hotjar Tracking Code for https://fulcrum.org -->
<script>
  // Wait until the cookie is set via user interaction
  // and only then add hotjar.
  (async() => {
    while(!Cookies.get('um_cookie_consent'))
      await new Promise(resolve => setTimeout(resolve, 1000));
    if (document.location.hostname === "fulcrum.org" || document.location.hostname === "www.fulcrum.org") {
      // HELIO-4209 only fulcrum.org proper, do not use hotjar with proxy servers
      setup_hotjar();
    }
  })();

  function setup_hotjar() {

    var allow_cookies = Cookies.get('um_cookie_consent') || 'na';

    if (allow_cookies !== 'deny' || allow_cookies !== 'dismiss') {

      (function(h,o,t,j,a,r){
          h.hj=h.hj||function(){(h.hj.q=h.hj.q||[]).push(arguments)};
          h._hjSettings={hjid:2863753,hjsv:6};
          a=o.getElementsByTagName('head')[0];
          r=o.createElement('script');r.async=1;
          r.src=t+h._hjSettings.hjid+j+h._hjSettings.hjsv;
          a.appendChild(r);
      })(window,document,'https://static.hotjar.com/c/hotjar-','.js?sv=');

    }
  }
</script>

<% end %>

<% if Settings.host == "heliotrope-preview.hydra.lib.umich.edu" %>
<!-- Hotjar Tracking Code for https://heliotrope-preview.hydra.lib.umich.edu/ -->
<script>
  // Wait until the cookie is set via user interaction
  // and only then add hotjar.
  (async() => {
    while(!Cookies.get('um_cookie_consent'))
      await new Promise(resolve => setTimeout(resolve, 1000));
    if (document.location.hostname === "heliotrope-preview.hydra.lib.umich.edu") {
      setup_hotjar();
    }
  })();

  function setup_hotjar() {

    var allow_cookies = Cookies.get('um_cookie_consent') || 'na';

    if (allow_cookies !== 'deny' || allow_cookies !== 'dismiss') {

      (function(h,o,t,j,a,r){
          h.hj=h.hj||function(){(h.hj.q=h.hj.q||[]).push(arguments)};
          h._hjSettings={hjid:2858980,hjsv:6};
          a=o.getElementsByTagName('head')[0];
          r=o.createElement('script');r.async=1;
          r.src=t+h._hjSettings.hjid+j+h._hjSettings.hjsv;
          a.appendChild(r);
      })(window,document,'https://static.hotjar.com/c/hotjar-','.js?sv=');

    }
  }
</script>
<% end %>